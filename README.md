# Buffer Factory - ([中文版README](./README-zh.md))
`node-factory` makes it no pain to create a Buffer object by encapsulate Buffer.from an new Buffer().

This module has been well unit tested and documented.